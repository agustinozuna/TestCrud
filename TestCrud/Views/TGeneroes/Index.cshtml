@model IEnumerable<TestCrud.Models.TGenero>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Crear</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
               Descripción
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TxtDesc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodGenero">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.CodGenero">Detalle</a> |
                <a asp-action="Delete" asp-route-id="@item.CodGenero">Borrar</a>
            </td>
        </tr>
}
    </tbody>
</table>
